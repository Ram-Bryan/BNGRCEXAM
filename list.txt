MISSION Refractor Besoins:
    - BesoinsController: utiliser Besoin update et non updateQuantite:
        - Flight request toutes les requetes et creer l'objet (pas de validation null ici mais validation null sera dans fonction update de Besoin.php)
    - Besoin.php model: utiliser update et non updateQuantite
        - modifier fonction update:
            - if != null -> sql.append 
    
    - DTOBesoins avec getters et setters pour chaque attributs 

    list.php:
        - Enlever les divs success ou errors et faire un div message ou on fait if (isset get Message) ou != null --> on echo et affiche. Donc dans chaque fonction du controller on doit mettre une variable message pour send vers besoin/list. Donc enlever les Flighr redirect simple pour aussi dispatcher les messages 
    
    edit.php:
        - same use GET 

        - utiliser les get de DTO comme $besoin->getID et non $besoin->id

        - ne pas utiliser fonction ucfirst 

    routes.php:
        - everything should be BesoinController::class and not new BesoinController() (we dont create instance because everything should be static for each cotnrollers inside this project)


    TypeArticle.php:
        - change the name TypeArticle::findAllArray to findAll 
    
    Dans form.php:
        - ne pas ecrire echo '</optgroup>' mais faire des ifs et ecrire html pure sans echo.

    Meilleur css pour http://localhost:1234/besoins/1/historique
    
Creer dans un todolist todolistt1.txt ces actions en liste avec temps estime a cotes: divise en vis a vis de pages, fonctions creer, fonctionnalites et DB requetes ou tables utilsier SEULEMENT POUR BESOINS avec les models ou tables relatives. SI ON RECOMMENCER LE PRJET DEPUIS 0 Exemple:
    - creer table besoins attributes(id,...) (5min)
    - creer table ville attribute(id, nom. region) (5min)
    - creer model Besoins (5mn)
    - creer model Ville (5min)
    - creer fonctoin create dans Besoins avec SQL: "INSERT INTO..." (1min)
    - creer vue... (10min)
Tu creera ce todolist comme si on reprenait tous le projet depuis le debut


Expactations:
    - refractored Besoin with the asked refractoring for simplicity and clarity
    - a full todo list of all the tasks
        
